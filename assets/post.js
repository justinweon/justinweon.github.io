function formatDate(value){const d=new Date(`${value}T00:00:00`);return Number.isNaN(d)?value:d.toLocaleDateString('en-GB',{day:'2-digit',month:'long',year:'numeric'}).toUpperCase()}
function renderMarkdown(source){const withoutTitle=(source||'').replace(/^\s*#\s+.+\r?\n+/, '');return marked.parse(withoutTitle,{gfm:true,breaks:false})}
function highlightCode(){document.querySelectorAll('.article-content pre code').forEach(block=>hljs.highlightElement(block))}
const id=new URLSearchParams(location.search).get('id');const target=document.getElementById('article');fetch('assets/posts.json').then(r=>r.json()).then(({posts})=>{const post=posts.find(item=>item.id===id);if(!post){target.innerHTML='<span class="label">NOT FOUND</span><h1>이 기록을 찾지 못했습니다.</h1><a class="back" href="/">← ARCHIVE</a>';return}document.title=`${post.title.replace(/<[^>]+>/g,' ')} — justin's notes`;target.innerHTML=`<a class="back" href="/">← ARCHIVE</a><span class="label">${post.category.toUpperCase()} / NOTE</span><h1>${post.title}</h1><div class="article-meta">DATE: ${formatDate(post.date)} &nbsp; / &nbsp; TAG: #${post.category}${post.source?' &nbsp; / &nbsp; SOURCE: TIL':''}</div><div class="article-content">${renderMarkdown(post.content)}</div>`;highlightCode()}).catch(()=>target.innerHTML='<span class="label">LOAD ERROR</span><h1>기록을 불러오지 못했습니다.</h1>');document.getElementById('theme-toggle').addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem('notes-theme',document.body.classList.contains('dark')?'dark':'light')});if(localStorage.getItem('notes-theme')==='dark')document.body.classList.add('dark');
